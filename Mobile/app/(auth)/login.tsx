import { StyleSheet, Text, View } from "react-native";

const loginPage = () => {
  return (
    <View>
      <Text>loginPage</Text>
    </View>
  );
};

export default loginPage;

const styles = StyleSheet.create({});
